#include<iostream>
#include<cstring>
using namespace std;
int main(){
	int n;
	cin>>n;
	int a[n];
	memset(a,-1,sizeof(a));        //-1 or 0 are only accepeted;
    cout<<a[1];
//	for(int i=0;i<n;i++)
	
}
